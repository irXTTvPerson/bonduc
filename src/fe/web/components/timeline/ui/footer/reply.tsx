import type { NextPage } from "next"
import styles from "../../../../styles/HTL.module.css"

const ReplyElement: NextPage = () => {
  return <span className={`${styles.article_container_footer_button}`}>◀</span>
}

export default ReplyElement
